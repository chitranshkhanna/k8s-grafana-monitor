{
  "name": "k8s-monitor-app",
  "version": "1.0.0",
  "description": "Node.js app with Prometheus metrics for Kubernetes monitoring",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo 'Tests passed' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^15.1.0"
  }
}